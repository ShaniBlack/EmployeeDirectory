import React from "react";
// import "./style.css"

const Header = () => {
    return (
        <header>
            <h1 className="text-center">Employee Directory</h1>
            <h2 className="text-center">Click on the column titles to filter by that column,
            or use the search bar to narrow your results</h2>
        </header>
    )
}

export default Header;